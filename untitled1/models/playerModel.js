var playerSchema = mongoose.Schema({
    // Using local for Local Strategy Passport

        name: String,
        prenom: String,
        age: Number,
        nationality: String,
        image: String,
        idTeams:Number,

    } )
